#include <iostream>
#include <clocale>
#include <math.h>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	int N, i;
	float j, R=1;
	cin >> N;
	for (i = 1; i <= N; i++)
	{
		j = i;
		R = R * 1 + R * (j / 10);
	}
	cout << R;
	return 0;
}
