#include <iostream>
#include <clocale>
#include <math.h>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	int A, B;
	cin >> A >> B;
	while (A != B)
	{
		if (A > B)
			A = A - B;
		else
			B = B - A;
	}
	cout << "НОД = " << A;
	return 0;
}
