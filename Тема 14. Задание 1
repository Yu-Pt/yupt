#include <iostream>
#include <clocale>
#include <math.h>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	int A, B, i, j;
	cin >> A >> B;
	for (i = B + 1; i < A; i++)
	{
		for (j = 0; j < i; j++)
			cout << i << " ";
		cout << endl;
	}
	return 0;
}
