#include <iostream>
#include <clocale>
#include <math.h>
using namespace std;

float PowerA3(float A) {
	float B;
	cin >> A;
	B = A * A * A;
	cout << B << endl;
	return B;
}

int main()
{
	setlocale(LC_ALL, "Russian");
	float A=0;
	PowerA3(A);
	PowerA3(A);
	PowerA3(A);
	PowerA3(A);
	PowerA3(A);
	return 0;
}
