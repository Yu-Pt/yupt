#include <iostream>
#include <clocale>
#include <math.h>
using namespace std;

int Sign(float X) {
	if (X < 0)
		return -1;
	if (X == 0)
		return 0;
	if (X > 0)
		return 1;
}

int main()
{
	setlocale(LC_ALL, "Russian");
	float A, B;
	cin >> A >> B;
	cout << Sign(A) + Sign(B);
	return 0;
}
