#include <iostream>
#include <clocale>
#include <math.h>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	int N, i;
	float R=0;
	cin >> N;
	for (i = 1; i <= N; i++)
	{
		R = (i * 2 - 1) + R;
	}
	cout << R;
	return 0;
}
