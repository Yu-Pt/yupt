#include <iostream>
#include <clocale>
#include <math.h>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	int A, B, i, j;
	cin >> A >> B;
	while (A >= B)
		A = A - B;
	cout << A;
	return 0;
}
